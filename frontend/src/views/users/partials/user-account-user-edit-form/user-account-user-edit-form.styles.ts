import { makeStyles } from '@mui/styles';
import { colors } from 'modules/common/utils/colors';

export const useUserAccountUserEditFormStyles = makeStyles(() => ({
  title: {
    fontWeight: 500,
    fontSize: '20px',
    lineHeight: '22px',
    color: colors.black,
    margin: '10px 0px',
  },
}));
