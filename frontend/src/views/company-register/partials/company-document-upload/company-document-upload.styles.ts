import { makeStyles } from '@mui/styles';

export const useCompanyDocumentUploadStyles = makeStyles(() => ({
  companyDocumentUploadHeader: {
    margin: '15px 0px',
  },
  companyDocumentUploadContainer:{
    padding: '0px 12px',
  }
}));
